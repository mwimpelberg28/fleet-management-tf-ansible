---
- name: Install and configure Grafana Alloy with Fleet Management remotecfg
  hosts: alloy_servers
  become: true

  vars_files:
    - vars/main.yml
    - vars/secrets.yml

  tasks:

    # ── 1. Install prerequisites ───────────────────────────────────────────
    - name: Install prerequisite packages (Debian/Ubuntu)
      apt:
        name:
          - apt-transport-https
          - software-properties-common
          - wget
          - gpg
          - curl
        state: present
        update_cache: true
      when: ansible_os_family == "Debian"

    - name: Install prerequisite packages (RedHat/Amazon Linux)
      dnf:
        name:
          - wget
          - gnupg2
          - curl
        state: present
      when: ansible_os_family == "RedHat"

    # ── 2. Add Grafana repository ──────────────────────────────────────────
    - name: Create keyrings directory (Debian/Ubuntu)
      file:
        path: /etc/apt/keyrings
        state: directory
        mode: '0755'
      when: ansible_os_family == "Debian"

    - name: Download Grafana APT GPG key (Debian/Ubuntu)
      get_url:
        url: https://apt.grafana.com/gpg.key
        dest: /tmp/grafana.gpg.key
        mode: '0644'
      when: ansible_os_family == "Debian"

    - name: Dearmor and install Grafana GPG key (Debian/Ubuntu)
      shell: |
        gpg --dearmor < /tmp/grafana.gpg.key > /etc/apt/keyrings/grafana.gpg
      args:
        creates: /etc/apt/keyrings/grafana.gpg
      when: ansible_os_family == "Debian"

    - name: Add Grafana APT repository (Debian/Ubuntu)
      apt_repository:
        repo: "deb [signed-by=/etc/apt/keyrings/grafana.gpg] https://apt.grafana.com stable main"
        state: present
        filename: grafana
      when: ansible_os_family == "Debian"

    - name: Import Grafana RPM GPG key (RedHat/Amazon Linux)
      rpm_key:
        key: https://rpm.grafana.com/gpg.key
        state: present
      when: ansible_os_family == "RedHat"

    - name: Add Grafana YUM repository (RedHat/Amazon Linux)
      yum_repository:
        name: grafana
        description: grafana
        baseurl: https://rpm.grafana.com
        gpgcheck: true
        gpgkey: https://rpm.grafana.com/gpg.key
        repo_gpgcheck: true
        enabled: true
      when: ansible_os_family == "RedHat"

    # ── 3. Install Grafana Alloy ───────────────────────────────────────────
    - name: Install Grafana Alloy (Debian/Ubuntu)
      apt:
        name: alloy
        state: present
        update_cache: true
      when: ansible_os_family == "Debian"

    - name: Install Grafana Alloy (RedHat/Amazon Linux)
      dnf:
        name: alloy
        state: present
      when: ansible_os_family == "RedHat"

    # ── 4. Create config directory ─────────────────────────────────────────
    - name: Ensure Alloy config directory exists
      file:
        path: "{{ alloy_config_dir }}"
        state: directory
        owner: alloy
        group: alloy
        mode: '0750'

    # ── 5. Deploy environment file ─────────────────────────────────────────
    - name: Deploy Alloy environment file
      template:
        src: templates/alloy.env.j2
        dest: "{{ alloy_env_file }}"
        owner: alloy
        group: alloy
        mode: '0640'
      notify: Restart Alloy

    # ── 6. Deploy Alloy remotecfg config ───────────────────────────────────
    - name: Deploy Alloy remotecfg configuration
      template:
        src: templates/config.alloy.j2
        dest: "{{ alloy_config_file }}"
        owner: alloy
        group: alloy
        mode: '0640'
      notify: Restart Alloy

    # ── 7. Configure systemd override ─────────────────────────────────────
    - name: Ensure systemd override directory exists
      file:
        path: /etc/systemd/system/alloy.service.d
        state: directory
        mode: '0755'

    - name: Deploy systemd override to set config path and env file
      copy:
        dest: /etc/systemd/system/alloy.service.d/override.conf
        content: |
          [Service]
          EnvironmentFile={{ alloy_env_file }}
          ExecStart=
          ExecStart=/usr/bin/alloy run --storage.path=/var/lib/alloy/data {{ alloy_config_file }}
        mode: '0644'
      notify:
        - Reload systemd
        - Restart Alloy

    # ── 8. Enable and start Alloy ──────────────────────────────────────────
    - name: Enable and start Alloy service
      systemd:
        name: alloy
        enabled: true
        state: started

  handlers:
    - name: Reload systemd
      systemd:
        daemon_reload: true

    - name: Restart Alloy
      systemd:
        name: alloy
        state: restarted
